---
const { href, title, action, class: className } = Astro.props
const style = action === 'primary' ? 'bg-primary' : 'bg-black'
---

<a
	href={href}
	class:list={[
		'text-accent px-5 py-4 text-4xl font-bold text-white selection:text-white',
		style,
		className
	]}>{title}</a
>
