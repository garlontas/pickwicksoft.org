---
interface Props {
	date: Date
}

const { date } = Astro.props
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('de-DE', {
			year: 'numeric',
			month: 'short',
			day: 'numeric'
		})
	}
</time>
