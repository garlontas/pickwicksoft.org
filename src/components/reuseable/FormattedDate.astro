---
interface Props {
	date: Date
	hideDay?: boolean
}

const { date, hideDay } = Astro.props
const dayConfig = hideDay ? {} : { day: 'numeric' }
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('de-DE', {
			year: 'numeric',
			month: 'short',
			...dayConfig
		})
	}
</time>
